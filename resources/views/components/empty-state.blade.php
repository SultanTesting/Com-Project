<div class="card">
    <div class="card-header">
        <h4>{{__("Whoops!")}}</h4>
    </div>
    <div class="card-body">
        <div class="empty-state" data-height="400">
        <div class="empty-state-icon">
            <i class="fas fa-question"></i>
        </div>
        <h2>{{__("We couldn't find any data")}}</h2>
        <p class="lead">
            {{__("Sorry we can't find any data, to get rid of this message, make at least 1 entry.")}}
        </p>
        <a href="#" class="mt-4 bb">{{__("Needing Help ?")}}</a>
        </div>
    </div>
</div>
